# Sample SKILL — minimal valid output template

Some prose. D1 may appear here freely because this is not in a fence.

<!-- lint:disable=R7 -->
```output-template
· D1(用户名是否区分大小写) → 不区分(85%)
· D2(密码长度下限) → 8 字符(90%)
```

More prose. Phase 3 mention here is fine because it's outside the fence.
